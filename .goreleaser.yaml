version: 2

project_name: suprsend
report_sizes: true
universal_binaries:
  - replace: true # remove the single-arch darwin binaries from artifacts
    # name_template: "{{ .ProjectName }}"  # optional, customize the output name
    hooks:
      post: cosign sign-blob --key "{{ .Env.KEY_URI }}" --bundle "{{ .Path }}.cosign.bundle" "{{ .Path }}" --yes

before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod tidy
    # you may remove this if you don't need go generate
    # - go generate ./...
    # - to generate build artifacts locally
    - make clean
    - make build

builds:
  - id: suprsend
    main: ./cmd/suprsend
    binary: suprsend
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X go.szostok.io/version.version={{.Version}}
      - -X go.szostok.io/version.buildDate={{.Date}}
    goarch: [amd64, arm64]
    goos:
      - linux
      - windows
      - darwin

archives:
  # existing tar/zip archives
  - id: tarballs
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

  # NEW: emit the raw binary as an artifact too
  - id: raw-binaries
    formats: binary
    # keep the same naming scheme so users can recognize platform/arch
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  ids: [tarballs]
  extra_files:
    - glob: "dist/**/*.cosign.bundle"

homebrew_casks:
  - name: suprsend
    ids: [tarballs]
    binary: suprsend
    directory: Casks
    url:
      verified: github.com/suprsend/cli
    homepage: https://github.com/suprsend/homebrew-tap
    commit_author:
      name: suprsend_bot
      email: bot@suprsend.com
    repository:
      owner: suprsend
      name: homebrew-tap
      token: "{{ .Env.GITHUB_TOKEN }}"
      git:
        url: git@github.com:suprsend/homebrew-tap.git
        private_key: "{{ .Env.PRIVATE_KEY_PATH }}"
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/suprsend"]
          end

notarize:
  macos:
    - # Whether this configuration is enabled or not.
      #
      # Default: false.
      # Templates: allowed.
      enabled: '{{ isEnvSet "MACOS_SIGN_P12" }}'

      # Before notarizing, we need to sign the binary.
      # This block defines the configuration for doing so.
      sign:
        # The .p12 certificate file path or its base64'd contents.
        #
        # Templates: allowed.
        certificate: "{{.Env.MACOS_SIGN_P12}}"

        # The password to be used to open the certificate.
        #
        # Templates: allowed.
        password: "{{.Env.MACOS_SIGN_PASSWORD}}"

      # Then, we notarize the binaries.
      #
      # You can leave this section empty if you only want
      # to sign the binaries (Since: v2.1).
      notarize:
        # The issuer ID.
        # Its the UUID you see when creating the App Store Connect key.
        #
        # Templates: allowed.
        issuer_id: "{{.Env.MACOS_NOTARY_ISSUER_ID}}"

        # Key ID.
        # You can see it in the list of App Store Connect Keys.
        # It will also be in the ApiKey filename.
        #
        # Templates: allowed.
        key_id: "{{.Env.MACOS_NOTARY_KEY_ID}}"

        # The .p8 key file path or its base64'd contents.
        #
        # Templates: allowed.
        key: "{{.Env.MACOS_NOTARY_KEY}}"

        # Whether to wait for the notarization to finish.
        # Not recommended, as it could take a really long time.
        wait: true

        # Timeout for the notarization.
        # Beware of the overall `--timeout` time.
        # This only has any effect if `wait` is true.
        #
        # Default: 10m.
        timeout: 20m

binary_signs:
  - id: cosign-kms-bundle
    # Put the bundle right next to the artifact:
    signature: '{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}{{ with .Arm }}v{{ . }}{{ end }}{{ with .Mips }}_{{ . }}{{ end }}{{ if not (eq .Amd64 "v1") }}{{ .Amd64 }}{{ end }}.cosign.bundle'
    cmd: cosign
    args:
      - sign-blob
      - "--key={{ .Env.KEY_URI }}" # e.g. awskms:///alias/suprsend-release
      - "--bundle" # cosign v3 requires bundle OR (sig+cert)
      - "${signature}" # writes the bundle here
      - "${artifact}" # the binary to sign
      - "--yes"
    artifacts: binary
signs:
  - id: checksums-cosign-kms
    signature: "${artifact}.cosign.bundle"
    cmd: cosign
    args:
      - sign-blob
      - "--key={{ .Env.KEY_URI }}"
      - "--bundle"
      - "${signature}" # bundle for the checksum artifact
      - "${artifact}" # usually the checksums.txt produced by GoReleaser
      - "--yes"
    artifacts: checksum
